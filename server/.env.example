# Example .env (DO NOT COMMIT REAL SECRETS)
# Use separate user/password variables (do not embed in DB_URL)

# Neon JDBC (no user/pass here)
DB_URL=jdbc:postgresql://ep-your-host-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require

# Credentials (replace in your local .env, never commit)
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# App secrets
JWT_SECRET=change_me_please

# CORS origin for the React client
FRONTEND_ORIGIN=http://localhost:5173
