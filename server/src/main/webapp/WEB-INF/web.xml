<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <display-name>Todo Servlet Backend</display-name>

    <!-- CORS Filter -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>com.example.todo.filter.CorsFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- JWT Auth Filter applied to protected API routes -->
    <filter>
        <filter-name>JWTAuthFilter</filter-name>
        <filter-class>com.example.todo.filter.JWTAuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>JWTAuthFilter</filter-name>
        <url-pattern>/api/todos/*</url-pattern>
    </filter-mapping>

    <!-- Auth Servlet -->
    <servlet>
        <servlet-name>AuthServlet</servlet-name>
        <servlet-class>com.example.todo.servlet.AuthServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AuthServlet</servlet-name>
        <url-pattern>/api/auth/*</url-pattern>
    </servlet-mapping>

    <!-- Todo Servlet -->
    <servlet>
        <servlet-name>TodoServlet</servlet-name>
        <servlet-class>com.example.todo.servlet.TodoServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>TodoServlet</servlet-name>
        <url-pattern>/api/todos/*</url-pattern>
    </servlet-mapping>

    <!-- Root JSON Servlet -->
    <servlet>
        <servlet-name>RootServlet</servlet-name>
        <servlet-class>com.example.todo.servlet.RootServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>RootServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <session-config>
        <session-timeout>1440</session-timeout>
    </session-config>

</web-app>
